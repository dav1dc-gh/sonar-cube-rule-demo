{
  "key": "inefficient-loops",
  "name": "Inefficient Loop Operations",
  "description": "Identifies performance issues in loops such as repeated method calls, unnecessary object creation, or operations that should be moved outside the loop.",
  "severity": "MAJOR",
  "type": "CODE_SMELL",
  "tags": ["performance", "optimization", "loops"],
  "remediation": {
    "constantCost": "10min",
    "examples": [
      {
        "before": "for (int i = 0; i < list.size(); i++) {\n  String upper = constantValue.toUpperCase();\n  process(upper);\n}",
        "after": "int size = list.size();\nString upper = constantValue.toUpperCase();\nfor (int i = 0; i < size; i++) {\n  process(upper);\n}"
      }
    ]
  },
  "impacts": [
    {
      "softwareQuality": "MAINTAINABILITY",
      "severity": "MEDIUM"
    }
  ],
  "defaultSeverity": "MAJOR",
  "status": "READY",
  "debt": {
    "function": "CONSTANT_ISSUE",
    "offset": "10min"
  }
}
