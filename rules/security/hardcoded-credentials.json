{
  "key": "hardcoded-credentials",
  "name": "Hardcoded Credentials Detection",
  "description": "Detects hardcoded passwords, API keys, tokens, or other sensitive credentials in source code, which poses a significant security risk.",
  "severity": "BLOCKER",
  "type": "VULNERABILITY",
  "tags": ["security", "credentials", "secrets", "owasp-top-10"],
  "remediation": {
    "constantCost": "15min",
    "examples": [
      {
        "before": "String password = \"mySecretPassword123\";",
        "after": "String password = System.getenv(\"DB_PASSWORD\");\n// Or use a secrets management service"
      }
    ]
  },
  "impacts": [
    {
      "softwareQuality": "SECURITY",
      "severity": "HIGH"
    }
  ],
  "defaultSeverity": "BLOCKER",
  "status": "READY",
  "debt": {
    "function": "CONSTANT_ISSUE",
    "offset": "15min"
  }
}
