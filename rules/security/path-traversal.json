{
  "key": "path-traversal",
  "name": "Path Traversal Vulnerability",
  "description": "Detects potential path traversal vulnerabilities where user input is used to construct file paths without proper validation, allowing attackers to access files outside the intended directory.",
  "severity": "CRITICAL",
  "type": "VULNERABILITY",
  "tags": ["security", "path-traversal", "owasp-top-10", "file-access"],
  "remediation": {
    "constantCost": "30min",
    "examples": [
      {
        "before": "File file = new File(baseDir + userInput);",
        "after": "Path resolvedPath = Paths.get(baseDir).resolve(userInput).normalize();\nif (!resolvedPath.startsWith(baseDir)) {\n  throw new SecurityException(\"Invalid path\");\n}"
      }
    ]
  },
  "impacts": [
    {
      "softwareQuality": "SECURITY",
      "severity": "HIGH"
    }
  ],
  "defaultSeverity": "CRITICAL",
  "status": "READY",
  "debt": {
    "function": "CONSTANT_ISSUE",
    "offset": "30min"
  }
}
